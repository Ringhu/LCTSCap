name: lctscap
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.12
  - pip
  - pip:
    - torch>=2.0
    - torchvision
    - transformers>=4.40
    - accelerate>=0.30
    - datasets
    - sentence-transformers>=3.0
    - evaluate
    - rouge-score
    - nltk
    - bert-score
    - pydantic>=2.0
    - pyyaml
    - wandb
    - tqdm
    - pandas>=2.0
    - pyarrow
    - scipy
    - scikit-learn
    - matplotlib
    - numpy
    - momentfm
    - pytest
    - ruff
